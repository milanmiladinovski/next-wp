type SiteConfig = {
  site_domain: string;
  site_name: string;
  site_description: string;
};

export const siteConfig: SiteConfig = {
  site_name: "Gardener Idea",
  site_description: "Explore your next gardening project",
  site_domain: "https://next-wp.com",
};
